version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./smartreco.db:/app/smartreco.db
      - ./chroma_data:/app/chroma_data
    restart: always
